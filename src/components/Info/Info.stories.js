import React from 'react';
import Info from './Info';

export default{
	title: 'Components/Info',
	component: Info
};

const Template = () => {
	return <Info/>
};

export const Default = Template.bind({});