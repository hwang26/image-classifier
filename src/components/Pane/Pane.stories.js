import React from 'react';
import Pane from './Pane';

export default{
	title: 'Components/Pane',
	component: Pane
};

const Template = () => {
	return <Pane/>
};

export const Default = Template.bind({});