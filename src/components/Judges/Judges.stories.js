import React from 'react';
import Judges from './Judges';

export default{
	title: 'Components/Judges',
	component: Judges
};

const Template = () => {
	return <Judges />
};

export const Default = Template.bind({});